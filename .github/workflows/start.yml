name: "START"

on:
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  orchestrate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: 执行工作流序列
        run: |
          # 定义任务列表：文件名|等待时间(秒)
          # 你可以在这里非常方便地编辑顺序和时间
          WORKFLOWS=(
            "gslege.yml|15"
            "vvhan.yml|15"
            "nirevil.yml|15"
            "mingyu.yml|15"
            "zhixuanwang.yml|15"
            "xinyitang3.yml|90"
            "vps789.yml|90"
            "tiancheng.yml|0"
          )

          for item in "${WORKFLOWS[@]}"; do
            IFS="|" read -r FILE WAIT_TIME <<< "$item"
            
            echo "---------------------------------------"
            echo "▶️ 正在启动: $FILE"
            
            gh workflow run "$FILE" --ref main
            
            if [ "$WAIT_TIME" -gt 0 ]; then
              echo "⌛ 等待 ${WAIT_TIME} 秒后执行下一个任务..."
              sleep "$WAIT_TIME"
            fi
          done
        env:
          GH_TOKEN: ${{ github.token }}