name: Chain Starter
on:
  workflow_dispatch:
permissions:
  actions: write
jobs:
  start:
    runs-on: ubuntu-latest
    steps:
      - name: Kick off
        run: gh workflow run tiancheng.yml --ref main
        env:
          GH_TOKEN: ${{ github.token }}